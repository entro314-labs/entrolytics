# Rename this file to .env and modify the values
#
# Connection string for Entrolytics’s database.
# If you use the bundled DB container, "db" is the hostname.
DATABASE_URL=postgresql://entrolytics:replace-me-with-a-random-string@db:5432/entrolytics

# Database type (e.g. postgresql)
DATABASE_TYPE=postgresql

# A secret string used by Entrolytics (replace with a strong random string)
APP_SECRET=replace-me-with-a-random-string

# Postgres container defaults.
POSTGRES_DB=entrolytics
POSTGRES_USER=entrolytics
POSTGRES_PASSWORD=replace-me-with-a-random-string
